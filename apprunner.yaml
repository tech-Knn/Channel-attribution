version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - npm ci --omit=dev

run:
  command: node src/index.js
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
